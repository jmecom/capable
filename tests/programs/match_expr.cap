module match_expr
use sys::system
use sys::console

pub fn main(sys: System) -> i32 {
  let c = system::console(sys)
  let v = match true {
    true => { "yes" }
    false => { "no" }
  }
  console::println(c, v)
  return 0
}

module match_expr
use sys::system
use sys::console

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  let v = match true {
    true => { "yes" }
    false => { "no" }
  }
  c.println(v)
  return 0
}

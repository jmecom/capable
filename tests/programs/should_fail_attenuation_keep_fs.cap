package safe
module should_fail_attenuation_keep_fs
use sys::system

pub fn main(rc: RootCap) -> i32 {
  let fs = rc.mint_filesystem("./")
  let d = fs.root_dir()
  let e = fs.root_dir()
  return 0
}

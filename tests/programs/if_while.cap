module if_while
use sys::system
use sys::console

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  if true { console::println(c, "yes") }
  while false { console::println(c, "loop") }
  return 0
}

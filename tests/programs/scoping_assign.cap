module scoping_assign
use sys::system

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  let x = 1

  if (true) {
    let y = x
    x = 2
  }

  c.assert(x == 2)
  c.println("x was mutated in inner scope")
  c.println("scoping assign test ok")
  return 0
}

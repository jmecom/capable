package safe
module text_to_string
use sys::system
use sys::string

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  let text = string::text_new()
  try text.push_byte('h') else {
    panic()
  }
  try text.push_byte('i') else {
    panic()
  }
  try let s = text.to_string() else {
    panic()
  }
  c.println(s)
  text.free()
  return 0
}

module match_bool
use sys::system
use sys::console

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  match true {
    true => { console::println(c, "yes") }
    _ => { console::println(c, "no") }
  }
  return 0
}

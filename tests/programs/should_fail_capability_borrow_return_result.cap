package safe
module should_fail_capability_borrow_return_result

capability struct Cap

impl Cap {
  pub fn try_dup(self: &Cap) -> Result<Cap, i32> {
    return Ok(Cap{})
  }
}

pub fn main() -> i32 {
  return 0
}

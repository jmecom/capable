module should_fail_for_non_i32_end
use sys::system
use sys::console

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()

  // Error: for loop range end must be i32
  for i in 0..true {
    c.println_i32(i)
  }

  return 0
}

module unit_match_arm
use sys::system

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  match true {
    true => { c.println("unit match arm ok") }
    false => { c.println("wrong branch") }
  }
  return 0
}

module unit_return
use sys::system

fn helper() -> unit {
  return ()
}

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  let x = 1
  helper()
  c.assert(x == 1)
  c.println("unit return ok")
  return 0
}

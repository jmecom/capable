module unit_return
use sys::system
use sys::console

fn helper() -> unit {
  return ()
}

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  helper()
  console::println(c, "unit return ok")
  return 0
}

module with_helper
use helper
use sys::system
use sys::console

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  let total = helper::add(1, 2)
  console::println(c, "ok")
  return total
}

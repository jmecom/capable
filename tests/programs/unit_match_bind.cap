module unit_match_bind
use sys::system

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  let flag = 0
  match () {
    x => {
      flag = 1
      c.println("unit match bind ok")
    }
  }
  sys.assert(flag == 1)
  return 0
}

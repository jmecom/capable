module unit_match_bind
use sys::system
use sys::console

pub fn main(sys: RootCap) -> i32 {
  let c = sys.mint_console()
  match () {
    x => { console::println(c, "unit match bind ok") }
  }
  return 0
}

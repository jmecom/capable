package safe
module args_safe
use sys::system

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  let args = rc.mint_args()
  let s = args.at_or(0, "")
  let m = s.len()
  c.assert(m >= 0)
  if (m < 0) {
    c.println("args bad")
    return 1
  }
  c.println("args ok")
  return 0
}

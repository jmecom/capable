package safe
module string_split
use sys::system

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  let parts = "a,b,c".split(',')
  let count = parts.len()
  parts.free()
  c.assert(count == 3)
  c.println("split ok")
  return 0
}

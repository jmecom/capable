/// Byte helpers and predicates.
package safe
module sys::bytes
impl u8 {
  /// True if the byte is ASCII whitespace.
  pub fn is_whitespace(self) -> bool {
    if (self == ' ' || self == '\t' || self == '\n' || self == '\r') {
      return true
    }
    return false
  }
}

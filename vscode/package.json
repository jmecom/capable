{
  "name": "capable-vscode",
  "displayName": "Capable",
  "description": "Capable language support (syntax + diagnostics)",
  "version": "0.1.0",
  "publisher": "capable",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:cap"
  ],
  "main": "./extension.js",
  "contributes": {
    "languages": [
      {
        "id": "cap",
        "aliases": [
          "Capable",
          "cap"
        ],
        "extensions": [
          ".cap"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "cap",
        "scopeName": "source.cap",
        "path": "./syntaxes/capable.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Capable",
      "properties": {
        "capable.capcPath": {
          "type": "string",
          "default": "capc",
          "description": "Path to the capc compiler used for diagnostics."
        },
        "capable.serverPath": {
          "type": "string",
          "default": "",
          "description": "Deprecated: use capable.capcPath instead."
        }
      }
    }
  },
  "scripts": {
    "lint": "echo \"no lint\"",
    "test": "echo \"no tests\""
  }
}

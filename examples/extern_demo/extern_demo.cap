package unsafe
module extern_demo
use sys.system
use sys.console

extern fn demo_log(msg: string) -> unit

pub fn main(sys: RootCap) -> i32 {
  let c = system.console(sys)
  demo_log("hello from extern demo")
  console.println(c, "capable still works")
  return 0
}

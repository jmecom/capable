package safe
module how_to_string
use sys::system
use sys::console
use sys::buffer

fn demo_string_view(c: Console) -> unit {
  let s = " hello,world \n"
  let trimmed = s.trim_view()
  c.println("-- string view --")
  c.println(trimmed)
  c.println("len:")
  c.println_i32(trimmed.len())

  try let parts = trimmed.split_once_view(',') else {
    c.println("comma not found")
    return
  }
  c.println(parts.left)
  c.println(parts.right)

  let words = trimmed.split_view_default(',')
  c.println("split count:")
  c.println_i32(words.len())
  c.println(words.get_or(0, ""))
  c.println(words.get_or(1, ""))
  words.free()
}

fn demo_text_builder(c: Console) -> unit {
  c.println("-- Text builder --")
  let t = buffer::text_new_default()
  try t.push_str("hello") else {
    panic()
  }
  try t.push_byte(' ') else {
    panic()
  }
  try t.append("text") else {
    panic()
  }
  try let head = t.slice_range(0, 5) else {
    panic()
  }
  c.println(head)
  try let owned = t.to_string() else {
    panic()
  }
  c.println(owned)
  t.free_default()

  try let t2 = buffer::text_from_default("owned") else {
    panic()
  }
  try let owned2 = t2.to_string() else {
    panic()
  }
  c.println(owned2)
  t2.free_default()

  try let joined = "a".concat_default("b") else {
    panic()
  }
  c.println(joined)
}

pub fn main(rc: RootCap) -> i32 {
  let c = rc.mint_console()
  demo_string_view(c)
  demo_text_builder(c)
  return 0
}
